'use strict';

/**
 * Constants
 * @type {{status: {FAILED: string, SUCCESS: string, USER_EXISTS: string}}}
 */
module.exports = {
  status: {
    FAILED: 'FAILED',
    SUCCESS: 'SUCCESS',
    USER_EXISTS: 'FAILED'
  }
};
