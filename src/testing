 canadaProvinces ={
    "01": "AB",
    "02": "BC",
    "03": "MB",
    "04": "NB",
    "05": "NL",
    "07": "NS",
    "08": "ON",
    "09": "PE",
    "10": "QC",
    "11": "SK",
    "12": "YT",
    "13": "NT",
    "14": "NU" }

console.log(canadaProvinces['01'])



/* let RBush = require('rbush')
var knn = require('rbush-knn');

var tree = new RBush(json.length); // create RBush tree
tree.load(json);

var neighbors = knn(tree, 43.70011, -79.4163,10, function(item){
  return item.name.indexOf('Londo') > -1 && item.population > 5000
}); 

console.log(neighbors) */


var neighbors = knn(tree, parsedQs.latitude, parsedQs.longitude,10, function(item){
        return item.name.indexOf('Londo') > -1 && item.population > 5000
      }); 