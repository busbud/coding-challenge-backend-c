import { GeolibInputCoordinates } from './types';
declare const isPointWithinRadius: (point: GeolibInputCoordinates, center: GeolibInputCoordinates, radius: number) => boolean;
export default isPointWithinRadius;
