var __$coverCall, __$coverInit, __$coverInitRange;
(function(fs){
	"use strict";
	var update = function(cb){
		return function(a, b, c, d){
			var json = JSON.parse(fs.readFileSync("/home/arian/www/prime/cov.json").toString() || '{}');
			cb(json, a, b);
			fs.writeFileSync("/home/arian/www/prime/cov.json", JSON.stringify(json, null, 2));
		};
	};
	__$coverInit = update(function(json, name, code){
		if (!json[name]) json[name] = {__code: code};
	});
	__$coverInitRange = update(function(json, name, range){
		if (!json[name][range]) json[name][range] = 0;
	});
	__$coverCall = update(function(json, name, range){
		json[name][range]++;
	});
})(require('fs'));
__$coverInit("array/indexOf.js", "/*\narray:indexOf\n*/\"use strict\"\n\nvar indexOf = function(self, item, from){\n    for (var l = self.length >>> 0, i = (from < 0) ? Math.max(0, l + from) : from || 0; i < l; i++){\n        if (self[i] === item) return i\n    }\n    return -1\n}\n\nmodule.exports = indexOf\n");
__$coverInitRange("array/indexOf.js", "19:32");
__$coverInitRange("array/indexOf.js", "33:237");
__$coverInitRange("array/indexOf.js", "238:262");
__$coverInitRange("array/indexOf.js", "79:219");
__$coverInitRange("array/indexOf.js", "225:234");
__$coverInitRange("array/indexOf.js", "184:218");
__$coverCall('array/indexOf.js', '19:32');
'use strict';
__$coverCall('array/indexOf.js', '33:237');
var indexOf = function (self, item, from) {
    __$coverCall('array/indexOf.js', '79:219');
    for (var l = self.length >>> 0, i = from < 0 ? Math.max(0, l + from) : from || 0; i < l; i++) {
        __$coverCall('array/indexOf.js', '184:218');
        if (self[i] === item)
            return i;
    }
    __$coverCall('array/indexOf.js', '225:234');
    return -1;
};
__$coverCall('array/indexOf.js', '238:262');
module.exports = indexOf;